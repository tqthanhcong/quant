on:
  schedule:
    - cron: '30 8 * * 1-5' # 18h VN, 11h UTC mỗi ngày làm việc
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
      - run: python files/noti/macd_divergence.py
